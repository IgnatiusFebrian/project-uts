<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kebutuhan Air</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">

    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <h2 class="mb-4">Kebutuhan Air Harian</h2>

        <form action="/water/calculate-water" method="POST">
            <div class="mb-3">
                <label for="weight" class="form-label">Berat Badan (kg):</label>
                <input type="number" name="weight" id="weight" min="0" placeholder="Masukkan berat badan" class="form-control" value="<%= input.weight || '' %>" required>
            </div>

            <div class="mb-3">
                <label for="activity" class="form-label">Tingkat Aktivitas:</label>
                <select name="activity" id="activity" class="form-control" required>
                    <option value="sedentary" <%= input.activity === 'sedentary' ? 'selected' : '' %>>Kurang</option>
                    <option value="light" <%= input.activity === 'light' ? 'selected' : '' %>>Ringan</option>
                    <option value="moderate" <%= input.activity === 'moderate' ? 'selected' : '' %>>Sedang</option>
                    <option value="active" <%= input.activity === 'active' ? 'selected' : '' %>>Aktif</option>
                    <option value="heavy" <%= input.activity === 'heavy' ? 'selected' : '' %>>Berat</option>
                </select>
            </div>

            <button type="submit" class="btn btn-primary">Hitung Kebutuhan Air</button>
            <a href="/water" class="btn btn-warning">Ulangi</a>
            <a href="/" class="btn btn-secondary">Kembali ke Home</a>
        </form>

        <% if (result) { %>
            <div class="mt-4">
                <h4>Kebutuhan Air Minum: <%= result.waterIntake.toFixed(2) %> liter/hari</h4>
            </div>
        <% } %>

        <% if (typeof waterIntake !== 'undefined') { %>
            <div class="mt-4 alert alert-info">
                <h4>Anda perlu minum sekitar <%= waterIntake.toFixed(2) %> liter air per hari.</h4>
            </div>
        <% } %>

        <% if (records && records.length > 0) { %>
        <% } %>
    </div>

    <footer class="text-center py-4 bg-white">
        <p>&copy; 2025 WeFit Indonesia</p>
    </footer>

</body>
</html>
