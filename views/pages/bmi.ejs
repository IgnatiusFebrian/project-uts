<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BMI Calculator</title>
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
  />
</head>
<body class="bg-light">

  <%- include('../partials/navbar') %>

  <div class="container mt-5">
    <h2 class="mb-4">BMI Calculator</h2>

    <form action="/bmi/calculate-bmi" method="POST">
      <div class="form-group">
        <label>Berat Badan (kg):</label>
        <input
          type="number"
          name="weight"
          class="form-control"
          step="0.1"
          min="0"
          placeholder="Masukkan berat badan"
          value="<%= input.weight || '' %>"
          required
        />
      </div>

      <div class="form-group">
        <label>Tinggi Badan (cm):</label>
        <input
          type="number"
          name="height"
          class="form-control"
          step="0.1"
          min="0"
          placeholder="Masukkan tinggi badan"
          value="<%= input.height || '' %>"
          required
        />
      </div>

      <button type="submit" class="btn btn-primary">Hitung BMI</button>
      <a href="/bmi" class="btn btn-warning">Ulangi</a>
      <a href="/" class="btn btn-secondary">Kembali ke Home</a>
    </form>

    <% if (result) { %>
      <div class="mt-4">
        <h4>BMI: <%= result.bmi %></h4>
        <h4>Kategori: <%= result.category %></h4>
      </div>
    <% } %>

    <% if (error) { %>
      <div class="alert alert-danger mt-3"><%= error %></div>
    <% } %>
  </div>

  <footer class="text-center py-4 bg-white">
    <p>&copy; 2025 WeFit Indonesia</p>
  </footer>

</body>
</html>
