<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rasio Lingkar Pinggang dan Pinggul (WHR)</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <h2 class="mb-4">Kalkulator WHR (Waist-to-Hip Ratio)</h2>

        <form action="/whr/calculate-whr" method="POST">

            <div class="form-group">
                <label>Lingkar Pinggang (cm):</label>
                <input type="number" name="waist" class="form-control" step="0.1" value="<%= input.waist || '' %>" required>
            </div>

            <div class="form-group">
                <label>Lingkar Pinggul (cm):</label>
                <input type="number" name="hip" class="form-control" step="0.1" value="<%= input.hip || '' %>" required>
            </div>

            <button type="submit" class="btn btn-primary">Hitung WHR</button>
            <a href="/whr" class="btn btn-warning">Ulangi</a>
            <a href="/" class="btn btn-secondary">Kembali ke Home</a>
        </form>

        <% if (whr) { %>
            <div class="mt-4">
                <h4>Rasio Lingkar Pinggang dan Pinggul (WHR): <%= whr.toFixed(2) %></h4>
                <p><%= whrStatus %></p>
            </div>
        <% } %>

        <% if (error) { %>
            <div class="alert alert-danger mt-3"><%= error %></div>
        <% } %>

    </div>
<footer class="text-center py-4 bg-white">
    <p>&copy; 2025 WeFit Indonesia</p>
</footer>
</body>
</html>
