<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Body Fat Calculator</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
  <%- include('../partials/navbar') %>

  <div class="container mt-5">
    <h2 class="mb-4">Body Fat Calculator</h2>

    <form action="/fat/calculate-fat" method="POST">
      <div class="form-group">
        <label>Lingkar Pinggang (cm):</label>
        <input
          type="number"
          name="waist"
          class="form-control"
          step="0.1"
          min="0"
          placeholder="Masukkan lingkar pinggang"
          value="<%= input.waist || '' %>"
          required
        >
      </div>

      <div class="form-group">
        <label>Lingkar Leher (cm):</label>
        <input
          type="number"
          name="neck"
          class="form-control"
          step="0.1"
          min="0"
          placeholder="Masukkan lingkar leher"
          value="<%= input.neck || '' %>"
          required
        >
      </div>

      <div class="form-group">
        <label>Tinggi Badan (cm):</label>
        <input
          type="number"
          name="height"
          class="form-control"
          step="0.1"
          min="0"
          placeholder="Masukkan tinggi badan"
          value="<%= input.height || '' %>"
          required
        >
      </div>

      <div class="form-group">
        <label>Lingkar Pinggul (cm):</label>
        <input
          type="number"
          name="hip"
          class="form-control"
          step="0.1"
          placeholder="Masukkan lingkar pinggul"
          value="<%= input.hip || '' %>"
        >
      </div>

      <button type="submit" class="btn btn-primary">Hitung Lemak Tubuh</button>
      <a href="/fat" class="btn btn-warning">Ulangi</a>
      <a href="/" class="btn btn-secondary">Kembali ke Home</a>
    </form>

    <% if (result && !isNaN(result.bodyFat)) { %>
      <div class="mt-4">
        <h4>Persentase Lemak Tubuh: <%= result.bodyFat %>%</h4>
      </div>
    <% } else if (error) { %>
      <div class="alert alert-danger mt-4"><%= error %></div>
    <% } %>
  </div>

  <footer class="text-center py-4 bg-white">
    <p>&copy; 2025 WeFit Indonesia</p>
  </footer>
</body>
</html>