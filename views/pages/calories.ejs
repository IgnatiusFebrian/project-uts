<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Calories Calculator</title>
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
  />
</head>
<body class="bg-light">

  <%- include('../partials/navbar') %>

  <div class="container mt-5">
    <h2 class="mb-4">Calories Calculator</h2>

    <form action="/calories/calculate-calories" method="POST">
      <div class="form-group">
        <label for="weight">Berat (kg):</label>
        <input
          type="number"
          id="weight"
          name="weight"
          min="0"
          placeholder="Masukkan berat badan"
          class="form-control"
          value="<%= input.weight || '' %>"
          required
        />
      </div>

      <div class="form-group">
        <label for="height">Tinggi (cm):</label>
        <input
          type="number"
          id="height"
          name="height"
          min="0"
          placeholder="Masukkan tinggi badan"
          class="form-control"
          value="<%= input.height || '' %>"
          required
        />
      </div>

      <div class="form-group">
        <label for="activity">Level Aktivitas:</label>
        <select
          id="activity"
          name="activity"
          class="form-control"
          required
        >
          <option value="">Pilih Level Aktivitas</option>
          <option value="sedentary" <%= input.activity === 'sedentary' ? 'selected' : '' %>>Kurang</option>
          <option value="light" <%= input.activity === 'light' ? 'selected' : '' %>>Ringan</option>
          <option value="moderate" <%= input.activity === 'moderate' ? 'selected' : '' %>>Sedang</option>
          <option value="active" <%= input.activity === 'active' ? 'selected' : '' %>>Aktif</option>
          <option value="very_active" <%= input.activity === 'very_active' ? 'selected' : '' %>>Berat</option>
        </select>
      </div>

      <button type="submit" class="btn btn-primary">Hitung Kalori</button>
      <a href="/calories" class="btn btn-warning ml-2">Ulangi</a>
      <a href="/" class="btn btn-secondary ml-2">Kembali ke Home</a>
    </form>

    <% if (result) { %>
      <div class="mt-4">
        <h4>Results</h4>
        <p>BMR: <%= result.bmr %> kcal/day</p>
        <p>TDEE: <%= result.tdee %> kcal/day</p>
      </div>
    <% } %>

  <footer class="text-center py-4 bg-white">
    <p>&copy; 2025 WeFit Indonesia</p>
  </footer>

</body>
</html>